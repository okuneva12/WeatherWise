# 🚀 Запуск WeatherWise через Git Bash

## Способ 1: Использование готового скрипта

### Вариант A: Запуск через двойной клик
1. Дважды кликните на файл `run_app.bat` в проводнике Windows
2. Скрипт автоматически найдет Git Bash и запустит приложение

### Вариант B: Запуск из Git Bash
1. Откройте Git Bash
2. Перейдите в папку проекта:
   ```bash
   cd /c/Users/Виктория/weatherwise_app
   ```
3. Сделайте скрипт исполняемым:
   ```bash
   chmod +x run_app.sh
   ```
4. Запустите скрипт:
   ```bash
   ./run_app.sh
   ```

## Способ 2: Прямые команды в Git Bash

### Шаг 1: Откройте Git Bash
Найдите "Git Bash" в меню Пуск и запустите его

### Шаг 2: Перейдите в папку проекта
```bash
cd /c/Users/Виктория/weatherwise_app
```

### Шаг 3: Проверьте Flutter
```bash
flutter --version
```

Если Flutter не найден, добавьте его в PATH:
```bash
export PATH=$PATH:/c/Users/Виктория/flutter/bin
```

*(Замените путь на реальный путь к вашему Flutter SDK)*

### Шаг 4: Получите зависимости
```bash
flutter pub get
```

### Шаг 5: Запустите приложение

**Варианты запуска:**

1. **Запуск на подключенном устройстве/эмуляторе:**
   ```bash
   flutter run
   ```

2. **Запуск в режиме отладки:**
   ```bash
   flutter run --debug
   ```

3. **Сборка APK (debug):**
   ```bash
   flutter build apk --debug
   ```

4. **Сборка APK (release):**
   ```bash
   flutter build apk --release
   ```

## Проверка устройств

Перед запуском проверьте доступные устройства:
```bash
flutter devices
```

Если устройство не подключено, подключите Android-устройство через USB с включенной отладкой или запустите Android эмулятор.

## Решение проблем

### Проблема: "flutter: command not found"
**Решение:** Добавьте Flutter в PATH:
```bash
export PATH=$PATH:/путь/к/flutter/bin
```

Чтобы сделать это постоянным, добавьте в `~/.bashrc`:
```bash
echo 'export PATH=$PATH:/c/Users/Виктория/flutter/bin' >> ~/.bashrc
source ~/.bashrc
```

### Проблема: Проблемы с кодировкой
Если видите искаженные символы в Git Bash, установите правильную кодировку:
```bash
export LANG=ru_RU.UTF-8
```

### Проблема: Ошибки сборки Gradle
Убедитесь, что Developer Mode включен в Windows (уже должно быть включено):
- Нажмите Win+I
- Перейдите в "Для разработчиков" (Developer Mode)
- Включите "Режим разработчика"

## Быстрые команды

```bash
# Очистка проекта
flutter clean

# Получение зависимостей
flutter pub get

# Анализ кода
flutter analyze

# Запуск приложения
flutter run

# Сборка APK
flutter build apk --release
```

