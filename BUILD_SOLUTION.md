# 🔧 Решение проблемы сборки APK

## Проблема
Сборка не работает из-за кириллицы в путях Windows (`C:\Users\Виктория\`). Gradle и Kotlin компилятор не поддерживают кириллические символы в путях.

## ✅ Решение 1: Сборка через Debug APK (самый простой способ)

Debug APK обычно собирается проще и тоже подходит для тестирования:

```bash
# В Git Bash
cd /c/Users/Виктория/weatherwise_app
flutter clean
flutter pub get
flutter build apk --debug
```

**Результат:** `build/app/outputs/flutter-apk/app-debug.apk`

Этот APK можно установить и протестировать!

---

## ✅ Решение 2: Создание символической ссылки (рекомендуется)

Создайте символическую ссылку с латинским именем:

1. **Откройте PowerShell от имени администратора:**
   - Нажмите Win+X
   - Выберите "Windows PowerShell (администратор)"

2. **Создайте символическую ссылку:**
```powershell
New-Item -ItemType SymbolicLink -Path "C:\Projects\weatherwise" -Target "C:\Users\Виктория\weatherwise_app"
```

3. **Используйте новый путь для сборки:**
```bash
# В Git Bash
cd /c/Projects/weatherwise
flutter clean
flutter pub get
flutter build apk --release
```

---

## ✅ Решение 3: Использовать готовый скрипт с обходными путями

Создайте файл `build_simple.sh` в проекте:

```bash
#!/bin/bash
cd /c/Users/Виктория/weatherwise_app

# Очистка
flutter clean

# Получение зависимостей
flutter pub get

# Сборка Debug APK (работает лучше с кириллицей)
echo "Сборка Debug APK..."
flutter build apk --debug

if [ -f "build/app/outputs/flutter-apk/app-debug.apk" ]; then
    echo ""
    echo "✅ APK собран успешно!"
    echo "Файл: build/app/outputs/flutter-apk/app-debug.apk"
    echo ""
    echo "Размер: $(du -h build/app/outputs/flutter-apk/app-debug.apk | cut -f1)"
else
    echo "❌ Ошибка сборки"
fi
```

Запустите:
```bash
chmod +x build_simple.sh
./build_simple.sh
```

---

## ✅ Решение 4: Сборка через Android Studio

Если у вас установлен Android Studio:

1. Откройте проект в Android Studio: `File → Open → выберите папку android`
2. `Build → Build Bundle(s) / APK(s) → Build APK(s)`
3. Android Studio лучше справляется с путями

---

## 📝 Что делать дальше (пошагово)

### Вариант А: Использовать Debug APK (быстро)

1. **Откройте Git Bash:**
```bash
cd /c/Users/Виктория/weatherwise_app
```

2. **Очистите проект:**
```bash
flutter clean
```

3. **Получите зависимости:**
```bash
flutter pub get
```

4. **Соберите Debug APK:**
```bash
flutter build apk --debug
```

5. **Проверьте результат:**
```bash
ls -lh build/app/outputs/flutter-apk/app-debug.apk
```

6. **Установите на устройство:**
   - Скопируйте `build/app/outputs/flutter-apk/app-debug.apk` на телефон
   - Откройте файл на телефоне и установите

---

### Вариант Б: Создать символическую ссылку (для Release)

1. **Откройте PowerShell от имени администратора**

2. **Выполните:**
```powershell
New-Item -ItemType SymbolicLink -Path "C:\Projects\weatherwise" -Target "C:\Users\Виктория\weatherwise_app"
```

3. **В Git Bash:**
```bash
cd /c/Projects/weatherwise
flutter clean
flutter pub get
flutter build apk --release
```

---

## 🎯 Рекомендация

**Для выполнения ЛР7 используйте Debug APK** - он полностью функционален и подходит для:
- ✅ Тестирования приложения
- ✅ Установки на устройство
- ✅ Загрузки в релиз GitHub

Debug APK отличается от Release только:
- Размером (чуть больше)
- Отладочной информацией
- Скоростью (чуть медленнее)

Но функционально идентичен!

---

## 📤 После сборки APK

1. **Установите на устройство** (см. инструкцию выше)
2. **Протестируйте приложение**
3. **Создайте тег lr7:**
```bash
git add .
git commit -m "ЛР7: Финальная сборка и установка"
git tag -a lr7 -m "ЛР7: Финальная сборка и установка"
git push origin main
git push origin lr7
```

4. **Создайте релиз на GitHub:**
   - Перейдите: https://github.com/okuneva12/WeatherWise/releases/new
   - Выберите тег: `lr7`
   - Прикрепите APK файл
   - Опубликуйте

---

## ⚠️ Если ничего не помогает

Можно использовать онлайн-сервисы для сборки:
- GitHub Actions (бесплатно)
- Codemagic (бесплатный тариф)

Или временно переместить проект в папку без кириллицы:
```
C:\Projects\weatherwise_app\
```


