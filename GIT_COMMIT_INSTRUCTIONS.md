# 📝 Инструкция по коммиту и созданию тега lr6

## ✅ Быстрый способ (используя скрипт)

1. **Откройте Git Bash** в папке проекта

2. **Выполните:**
```bash
chmod +x commit_and_tag_lr6.sh
./commit_and_tag_lr6.sh
```

3. **Следуйте инструкциям** скрипта (подтвердите действия)

---

## 📋 Ручной способ (пошагово)

### Шаг 1: Откройте Git Bash
```bash
cd /c/Users/Виктория/weatherwise_app
```

### Шаг 2: Добавьте все изменения
```bash
git add .
```

### Шаг 3: Проверьте, что будет закоммичено
```bash
git status
```

### Шаг 4: Создайте коммит
```bash
git commit -m "ЛР6: Работа с данными (БД и HTTP API)

- Реализовано локальное хранение через SharedPreferences
- Интегрирован OpenWeatherMap API
- Данные отображаются в интерфейсе
- Реализована архитектура по Flutter guidelines:
  * Repository слой
  * Service слой (WeatherService, StorageService)
  * ViewModel слой
  * Dependency Injection через GetIt
- Улучшена обработка ошибок
- Исправлены все предупреждения линтера"
```

### Шаг 5: Отправьте на GitHub
```bash
git push origin main
```
*(замените `main` на название вашей ветки, если отличается)*

### Шаг 6: Создайте тег lr6
```bash
git tag -a lr6 -m "Лабораторная работа 6: Работа с данными (БД и HTTP API)"
```

### Шаг 7: Отправьте тег на GitHub
```bash
git push origin lr6
```

---

## 🔍 Проверка результата

После выполнения всех шагов проверьте:

```bash
# Проверка последнего коммита
git log -1

# Проверка тегов
git tag -l

# Проверка статуса
git status
```

Также проверьте на GitHub:
- Все изменения должны быть в репозитории
- Должен появиться тег `lr6` в разделе Releases/Tags

---

## ⚠️ Если тег уже существует

Если тег `lr6` уже существует и вы хотите его пересоздать:

```bash
# Удалить локальный тег
git tag -d lr6

# Удалить тег на GitHub
git push origin :refs/tags/lr6

# Создать новый тег
git tag -a lr6 -m "Лабораторная работа 6: Работа с данными (БД и HTTP API)"

# Отправить новый тег
git push origin lr6
```

---

## 📁 Что будет закоммичено

- ✅ Все изменения в коде (services, repositories, viewmodels, screens)
- ✅ Новая структура DI
- ✅ Обновленные зависимости (pubspec.yaml)
- ✅ Исправления в Android (Gradle, AndroidManifest)
- ✅ Скрипты для запуска
- ✅ Документация

